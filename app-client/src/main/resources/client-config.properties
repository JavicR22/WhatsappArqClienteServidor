# Configuración del servidor
server.host=localhost
server.port=8080
mock.enabled=false

# Configuración de la base de datos H2
db.url=jdbc:h2:./data/clientdb;AUTO_SERVER=TRUE
db.user=sa
db.password=
maxConnections=5

# Configuración de sesión
session.timeout.minutes=30
session.persist=true
